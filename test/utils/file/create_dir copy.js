import { strictEqual } from 'assert';
import { describe, it } from 'mocha';
// import { read_json } from '../../../src/utils/file.js';
import { v4 } from 'uuid';
import { dirname, join } from 'path';
import { existsSync, rmSync } from 'fs';

describe('utils/file/read_json', () => {
    // it('single', () => {
    // });
});
